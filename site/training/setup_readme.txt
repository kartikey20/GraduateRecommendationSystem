#install python dependencies (with pip 3):

pip install flask
pip install flask-cors

#run python backend file
python app.py

#start server separately
python -m http.server

#load site on localhost:8000

note: set variable 'debug' = true if testing, but don't push it with it set to true.